function findMissingLetter(input) {
  // create array with the inputs' charcodes
  // look at charcode of first element
  // increase charcode + 1
  // if the charccode is in the array
  // increase charcode + 1
  // else return its char
}
module.exports = findMissingLetter;
